# รายงานปฏิบัติการที่ 9 (Lab 09)

**ชื่อ - นามสกุล :** วราวุฒิ จำนงค์ผล
**รหัสนิสิต :** 67160197

---

## 📋 คำถามทดสอบความเข้าใจ (Assessment Questions)

## ส่วนที่ 1: การทำความเข้าใจโค้ด

### คำถาม 1.1 เหตุใดจึงต้องแยก duration และ timeLeft ออกจากกัน อธิบายความแตกต่าง

- ตอบ : เพราะว่าต้องมีตัวแปรตั้งต้นไม่งั้นเวลานับถอยหลังมันจะไม่สามารถรู้ค่าเริ่มต้นที่ตั้งไว้ได้ เพราะมันจะถูกลดลงเรื่อย ๆ ดังนั้นจึงถูกแยกออกมาเป็น duration และ timeLeft ความแตกต่างก็คือ duration เป็น reference ว่า task นี้ถูกตั้งไว้ให้ใช้เวลานานเท่าไร ใช้เก็บ "เวลาตั้งต้น" และแปลงเป็นวินาที ส่วน timeLeft ใช้เก็บ "เวลาที่เหลืออยู่จริง" ระหว่างการนับถอยหลังและเปลี่ยนแปลงตลอดเวลา จนเหลือ 0

### คำถาม 1.2 formatTime() ฟังก์ชันนี้ทำความสำคัญอะไร? ถ้าไม่มีฟังก์ชันนี้ app จะเป็นยังไง

- ตอบ : มีสำคัญในการแปลงเวลาจากวินาทีเป็นนาทีที่อ่านเข้าใจได่ง่าน และถ้าไม่มี formatTime() ถ้าเวลาเป็น 1800 ก็แสดงเป็นแบบนั้นเลยทำให้เข้าใจได้ยาก ทำให้ผู้ใช้ต้องทำความเข้าใจเอง

### คำถาม 1.3 อธิบายข้อแตกต่างระหว่าง setInterval() และ setTimeout()

- ตอบ : setInterval() ใช้สำหรับการทำงาน ซ้ำๆ ทุกๆ ตามระยะเวลาที่กำหนด ไปจนกว่าจะหยุดด้วยคำฟังก์ชัน clearInterval() ส่วน setTimeout() ใช้สำหรับการหน่วงเวลาแล้วทำงาน ครั้งเดียว หลังจากเวลาที่กำหนดไว้ภายใต้คำสั่ง

---

## ส่วนที่ 2: ฟังก์ชันการทำงาน

### คำถาม 2.1 เมื่อผู้ใช้คลิกปุ่ม “▶️ เริ่ม” ครั้งแรก เกิดอะไรขึ้นในลำดับใด (ระบุขั้นตอนอย่างน้อย 4 ขั้น)

- ตอบ : จะเกิดเหตุการณ์ต่างดังนี้ไล่ไปตามขั้นตอน
  - ขั้นตอนแรก 1.Event Listener ทำงาน ปุ่มถูกผูกกับ onclick ="toggleTimer(id)" จะเรียกฟังก์ชัน toggleTimer() พร้อมส่ง idของtask ที่เกี่ยวข้องเข้าไป
  - ขั้นตอนสอง 2.ทำการตรวจสอบสถานะของ task ด้วย tasks.find()พบว่า isRunningเพราะtaskนั้นยังเป็นสถานะfalseเพราะเพิ่งสร้างขึ้นมาใหม่
  - ขั้นตอนสาม 3.เริ่มต้นการนับเวลาโค้ดเข้าสู่เงื่อนไขการทำงานและตรวจสอบ else ตั้งค่า task.isRunning = true เมื่อเริ่มเวลาสร้างตัวจับเวลา setInterval() ให้ทำงานทุก ๆ 1 วินาทีตามที่ตั้งไว้
  - ขั้นตอนสี่ 4.การทำงานของ setInterval() โดยทุกๆ 1 วินาที task.timeLeft-- จะถูกลดลงทีละ 1 แต่ถ้าเมื่อtimeLeftลดจนเหลือ 0 หรือหมดเวลา หยุดการทำงานด้วย clearInterval() ตั้งค่า isRunning = false เมื่อเวลาหมด เล่นเสียง alarm (playAlarm()) และแสดง notification ว่าเสร็จสิ้น
  - ขั้นตอนห้า 5.ทุกครั้งที่เวลาลดลง จะเรียก renderTasks() เพื่ออัปเดตหน้าจอให้ผู้ใช้เห็นเวลาที่เหลือแบบเรียลไทม์

### คำถาม 2.2 ในฟังก์ชัน toggleTimer() ทำไมจึงต้องมี clearInterval() ก่อนแล้วจึงตั้ง task.isRunning = false

- ตอบ : clearInterval() คือการหยุดการทำงานจริงของตัวจับเวลาในของส่วน เพื่อป้องกันเวลาใน task ลดลงต่อเนื่องโดยไม่ถูกควบคุมแล้วจึงไปทำในส่วนtask.isRunning = false คือการอัปเดตสถานะของ task ให้ตรงกับความจริง การทำสองขั้นตอนนี้ต่อเนื่องกันทำให้ทั้ง การทำงานจริง และ สถานะที่แสดงผล สอดคล้องกัน ไม่เกิดบั๊กหรือความผิดพลาดใน UI

### คำถาม 2.3 ถ้า timer หมดเวลา จะเกิดความผิดพลาดใดหากไม่มี clearInterval()

- ตอบ : หากไม่มี clearInterval() เมื่อ timer หมดเวลา จะทำให้ตัวจับเวลายังทำงานต่อไปทั้งที่ควรหยุดแล้วทำให้ส่งผลเกิดการทำงานซ้ำซ้อนต่อเนื่องไม่มีหยุดทำให้เกิดปัญหาขึ้นมาได้ เช่น UI กระพริบ, alarm เด้งไม่หยุด, notification ซ้ำ และสิ้นเปลืองทรัพยากรโดยไม่จำเป็น

---

## ส่วนที่ 3: DOM และ UI

### คำถาม 3.1 ฟังก์ชัน renderTasks() สามารถแยกออกเป็นฟังก์ชันย่อยได้กี่ฟังก์ชัน จงเสนอการแยกที่เหมาะสม

- ตอบ : renderTasks() สามารถแยกออกเป็น 4 ฟังก์ชันย่อยหลัก ได้แก่
  - 1.getTaskStatus(task) ตรวจสอบว่า task เสร็จแล้ว,ใกล้หมดเวลา
  - 2.renderTaskItem(task) รับtask 1 ตัว แล้วคืนค่าHTML string ของtaskเหล่านั้นได้
  - 3.renderTaskControls(task)สร้างHTMLของปุ่ม “▶️ เริ่ม/⏸️ หยุด” และ “ลบ” โดยตรวจสอบสถานะ isRunning และ isCompleted
  - 4.renderTaskList(tasks)รวมHTMLของทุก task ด้วยการเรียก renderTaskItem() แล้วใส่ลงใน taskList.innerHTML

### คำถาม 3.2 ใช้วิธีใดในการอัปเดต UI ทุกครั้งที่ timer เปลี่ยนแปลง มีวิธีการอื่นนอกเหนือจำนวนปัจจุบันหรือไม่

- ตอบ : ทุกครั้งที่เวลาเปลี่ยนแปลง จะมีการเรียก renderTasks() เพื่ออัปเดต UI ให้ตรงกับสถานะล่าสุดของ task วิธีการอื่นนอกเหนือคือ
  - 1.อัปเดตเฉพาะ DOM element ที่เปลี่ยนแปลง เช่น อัปเดตเฉพาะ <div class="task-timer"> ของ task ที่กำลังนับถอยหลัง
  - 2.ใช้Virtual DOM หรือ Frameworkให้frameworkจัดการ diff และ update เฉพาะส่วนที่เปลี่ยน
    วิธีการพวกนี้อยู่ที่การปรับเปลี่ยนให้โค้ดมีความหยืดหยุ่นและอัปเดตได้มีประสิทธิภาพมากขึ้น

### คำถาม 3.3 “keypress” event listener ทำความสำคัญอะไร หากลบออกไป app จะเป็นยังไง

- ตอบ : keypress ใช้ใน event listener ทำให้ผู้ใช้สามารถเพิ่ม task ได้ด้วยการกด Enter ซึ่งช่วยให้การใช้งานรวดเร็วและสะดวกขึ้นทำให้การทำงานของหน้าเว็บดูกระชับ แต่ถ้าลบออกไป แอปยังทำงานได้ตามปกติ แต่ผู้ใช้จะต้องคลิกปุ่ม “เพิ่ม” ทุกครั้งแทน ซึ่งอาจทำให้เสียเวลาขึ้นเล็กน้อย

---

## ส่วนที่ 4: Web Audio API

### คำถาม 4.1 อธิบายหน้าที่ของการตั้งค่า:

- **oscillator.frequency.value = 800**
  - ตอบ : กำหนดความถี่ของเสียงที่ oscillator สร้างขึ้นเป็น 800 Hz ความถี่นี้คือระดับเสียงที่ผู้ใช้จะได้ยินตามตัวเลข ยิ่งตัวเลขสูง เสียงยิ่งแหลม, ยิ่งต่ำ เสียงยิ่งทุ้ม
- **oscillator.type = "sine"**
  - ตอบ : กำหนดรูปคลื่นเสียงเป็น sine wave หรือคลื่นไซน์ ให้เสียงที่นุ่มและบริสุทธิ์ที่สุดต่างจาก square, sawtooth, triangle ที่ให้เสียงหยาบหรือมีฮาร์มอนิกมากกว่า
- **gainNode.gain.setValueAtTime(0.3, audioContext.currentTime)**
  - ตอบ : กำหนดระดับความดังของเสียงที่ออกมาเป็น 0.3 (จากช่วง 0.0–1.0) ณ เวลาปัจจุบัน

### คำถาม 4.2 ทำไมเสียง alarm จึงใช้เวลา 0.5 วินาที แล้วหยุด ถ้าเพิ่มเป็น 2 วินาทีจะส่งผลต่อ UX อย่างไร

- ตอบ : เสียง alarm ที่ยาว 0.5 วินาที เหมาะกับการแจ้งเตือนแบบสั้น กระชับ และไม่รบกวน แต่ถ้าเพิ่มเป็น 2 วินาที จะทำให้ผู้ใช้สังเกตได้ชัดขึ้น แต่ก็เสี่ยงต่อการสร้างความรำคาญและลดความสบายในการใช้งาน ขึ้นอยู่กับผู้ใช้งานว่าต้องการระยะเวลาของเสียงเท่าไหร่กำหนดได้ตามต้องการ

---

## ส่วนที่ 5: Array Methods

### คำถาม 5.1 ในฟังก์ชัน deleteTask(id) ใช้ filter() เพื่ออะไร เขียนวิธีอื่นแทน filter() ได้หรือไม่

- ตอบ : filter() ใช้สำหรับสร้างอาร์เรย์ใหม่ที่ตัด task ที่ต้องการลบออกไป โดยไม่แก้ไขอาร์เรย์เดิมโดยตรง ซึ่งมีส่วนช่วยที่สั้น กระชับ และอ่านง่ายที่สุด สำหรับการลบแต่สามารถเขียนวิธีอื่นแทนได้เช่น splice(), for loop, reduce() ขึ้นอยู่กับผู้เขียนว่าจะประยุกต์ใข้งานกับการทำงานของโค้ดได้อย่างเหมาะสม

### คำถาม 5.2 ในฟังก์ชัน renderTasks() ใช้ map() เพื่อทำอะไร ถ้าใช้ forEach() แทนจะดีหรือไม่

- ตอบ : ใช้ map() ทำการวนลูปผ่านทุก task ในอาร์เรย์ tasks คืนค่าเป็น อาร์เรย์ใหม่ ที่ประกอบด้วย HTML string ของแต่ละ taskจากนั้นใช้ .join("") รวมเป็น string เดียว → ใส่ลงใน taskList.innerHTML เพื่ออัปเดต UI แต่ถ้าใช้ forEach() แทนวนลูปเช่นกัน แต่ ไม่คืนค่าอาร์เรย์ใหม่ ใช้งานได้ แต่โค้ดจะยาวขึ้นและไม่กระชับเท่า map()

---

## ส่วนที่ 6: ข้อบกพร่องและการแก้ไข

### คำถาม 6.1 หากผู้ใช้เพิ่ม task 2 อัน แล้วคลิก “เริ่ม” บน task ทั้ง 2 อันพร้อมๆ กัน จะเกิดอะไรขึ้น

- ตอบ : จะมี 2 interval ทำงานคู่ขนาน แต่ละ task นับถอยหลังและอัปเดต UI ของตัวเองโดยไม่รบกวนกัน เมื่อหมดเวลาแต่ละ task ก็จะหยุดทำงานและแจ้งเตือนแยกกัน ทำให่ไม่สับสนการทำงานในหน้าเว็บ

### คำถาม 6.2 ถ้า timer กำลังเดินอยู่ และผู้ใช้รีเฟรชหน้า (F5) task จะหายไป ทำไม วิธีแก้ปัญหาคือ

- ตอบ : ตัวแปร tasks ถูกเก็บไว้ในหน่วยความจำของ JavaScript (RAM) เท่านั้นเมื่อรีเฟรชหน้าเว็บ เบราว์เซอร์จะโหลดไฟล์ HTML/JS ใหม่จะทำให้ตัวแปรทั้งหมดถูกรีเซ็ตกลับเป็นค่าเริ่มต้น (tasks = []) วิธีแก้คือเพื่อไม่ให้ task หายไปเมื่อรีเฟรชสามารถใช้วิธีเก็บข้อมูลถาวรเช่น localStorage, IndexedDB, หรือฐานข้อมูลจริง

### คำถาม 6.3 ในการทดสอบ app ที่ timer 30 นาที จะใช้เวลานานไปหรือไม่ เสนอวิธีทดสอบที่เร็วขึ้น

- ตอบ : จริงๆ จะใช้เวลานานมากเกินไปสำหรับการตรวจสอบฟังก์ชัน เพราะนักพัฒนาต้องรอจนกว่าจะครบ 30 นาทีถึงจะเห็นผลลัพธ์ ทำให้การทดสอบช้าและไม่คุ้มค่าเวลา วิธีทดสอบที่เร็วขึ้น ลดเวลาในการทดสอบตั้งค่า timer ให้สั้นลง เช่น 30 วินาที หรือ 1 นาที เพื่อตรวจได้รวดเร็วและหาปัญหาได้รวดเร็ว

---

## ส่วนที่ 7: Enhancement

### คำถาม 7.1 หากต้องการเพิ่มฟังก์ชัน “รีเซ็ต timer” คืนไปเป็น 30 นาที โค้ดจะเป็นยังไง

- ตอบ : หน้าตาโค้ดจะออกมาหน้าตาประมาณนี้
  - function resetTimer(id) {
  - const task = tasks.find((t) => t.id === id);
  - if (!task) return;

  - // หยุด interval เดิมก่อน
  - if (task.intervalId) {
  - clearInterval(task.intervalId);
  - task.intervalId = null;
  - }

  - // รีเซ็ตค่าเวลาและสถานะ
  - task.timeLeft = 30 \* 60; // 30 นาที = 1800 วินาที
  - task.isRunning = false;

  - // อัปเดต UI
  - renderTasks();
  - }
  - และปุ่มตอนรีเซ็ทการใช้งาน
    <button class="btn-reset" onclick="resetTimer(${task.id})">
    🔄 รีเซ็ต
    </button>
  - การทำงานคือ
    - ตั้งค่า timeLeft กลับไปเป็น 1800 วินาที (30 นาที เวลาตั้งต้น)
    - ตั้งค่า isRunning = false เพื่อให้สถานะกลับไปเป็น “ยังไม่เริ่ม”
    - เรียก renderTasks() เพื่ออัปเดต UI ให้ผู้ใช้เห็นเวลาใหม่

### คำถาม 7.2 วิธีใดในการเก็บ tasks ไว้ใน LocalStorage เพื่อไม่ให้หายเมื่อรีเฟรชหน้า

- ตอบ : การใช้ LocalStorage คือวิธีง่ายและตรงไปตรงมาในการเก็บ tasks ให้คงอยู่แม้รีเฟรชหน้า โดยใช้ JSON.stringify() เพื่อบันทึก และ JSON.parse() เพื่อโหลดกลับมา เป็นวิธี่การแก้ไขเมื่อการกดรีเฟรชหรือF5 เพื่อป้องนี้เป็นวิธีที่ใช้งานดี

### คำถาม 7.3 ถ้าต้องการให้ผู้ใช้กำหนดเวลา timer เอง จะต้องแก้ไขฟังก์ชันไหนบ้าง

- ตอบ : ต้องไปแก้ไขที่โค้ด function addTask()
  - const newTask = {
  - id: Date.now(),
  - name,
  - timeLeft: minutes \* 60, // ใช้ค่าที่ผู้ใช้กำหนด
  - isRunning: false,
  - intervalId: null, };
  - ส่วนสำคัญ ที่ต้องแก้
    - const newTask = {
    - id: Date.now(),
    - name,
    - timeLeft: minutes \*60,
    - initialTime: minutes \*60, // เก็บค่าเริ่มต้นไว้
    - isRunning: false,
    - intervalId: null,
    - };
  - แค่นั้ก็สามารถเปลี่ยนเวลาให้เปลี่ยนไปได้ตามที่ต้องการ

---

## ส่วนที่ 8: Concepts เชิงลึก

### คำถาม 8.1 ทำไม renderTasks() จึงต้องเรียกหลายครั้ง มีวิธี DRY ที่ดีกว่าหรือไม่

- ตอบ : renderTasks() ต้องถูกเรียกหลายครั้ง เพราะทุกครั้งที่มีการเปลี่ยนแปลงข้อมูลของ task (เช่น เพิ่ม, ลบ, เริ่ม, หยุด, เวลาลดลง) UI ต้องถูกอัปเดตให้ตรงกับสถานะล่าสุดเสมอ สามารถDRYได้ตามที่ต้องการควรสร้างฟังก์ชันกลาง เช่น updateTasks() หรือใช้ framework ที่มี data binding จะลดการเรียกซ้ำและทำให้โค้ดสะอาดขึ้นทำให้ลดการเกิด bug

### คำถาม 8.2 ถ้าเปลี่ยนจาก setInterval เป็น requestAnimationFrame จะส่งผลดีหรือไม่

- ตอบ : ไปใช้ requestAnimationFrame มีผลดีและข้อควรพิจารณาแตกต่างกัน ขึ้นอยู่กับลักษณะการใช้งานของ timer:
  - การทำงาน
    - setInterval เรียก callback ทุก ๆ X ms ตามที่กำหนด (เช่น 1000ms)
    - requestAnimationFrame เรียก callback ทุกครั้งที่ browser พร้อมจะวาดเฟรมใหม่ (sync กับ refresh rate ~60fps) ,
  - ความแม่นยำเวลา
    - setInterval อาจไม่แม่นยำถ้า browser ช้า หรือ tab ไม่ active
    - requestAnimationFrame แม่นยำสำหรับ animation เพราะ sync กับการ render ของ browser
  - การใช้ทรัพยากร
    - setInterval ทำงานต่อเนื่องแม้ browser ไม่พร้อมทำให้อาจสิ้นเปลือง
    - requestAnimationFrame ประหยัดกว่า เพราะทำงานเฉพาะตอน browser วาดเฟรม

### คำถาม 8.3 ในโค้ดที่ใช้ setInterval() มี Memory Leak ที่อาจเกิดขึ้นได้หรือไม่

- ตอบ : ถ้าใช้ setInterval() โดยไม่เคลียร์เมื่อ task หยุดหรือลบอาจมีความเสี่ยงที่จะเกิด Memory Leak ได้ถ้าไม่ได้จัดการอย่างถูกต้อง โดยเฉพาะในกรณีที่มีการสร้าง interval หลายครั้งแต่ไม่เคยลบออกเลย วิธีป้องกัน Memory Leak ใช้ clearInterval() ทุกครั้งที่หยุด/ลบ task และป้องกันการสร้าง interval ซ้ำ

---
