# รายงานปฏิบัติการที่ 9 (Lab 09)

**ชื่อ - นามสกุล :** วราวุฒิ จำนงค์ผล
**รหัสนิสิต :** 67160197

---

## 📋 คำถามทดสอบความเข้าใจ (Assessment Questions)

## ส่วนที่ 1: การทำความเข้าใจโค้ด

### คำถาม 1.1 เหตุใดจึงต้องแยก duration และ timeLeft ออกจากกัน อธิบายความแตกต่าง

- ตอบ : เพราะว่าต้องมีตัวแปรตั้งต้นไม่งั้นเวลานับถอยหลังมันจะไม่สามารถรู้ค่าเริ่มต้นที่ตั้งไว้ได้ เพราะมันจะถูกลดลงเรื่อย ๆ ดังนั้นจึงถูกแยกออกมาเป็น duration และ timeLeft ความแตกต่างก็คือ duration เป็น reference ว่า task นี้ถูกตั้งไว้ให้ใช้เวลานานเท่าไร ใช้เก็บ "เวลาตั้งต้น" และแปลงเป็นวินาที ส่วน timeLeft ใช้เก็บ "เวลาที่เหลืออยู่จริง" ระหว่างการนับถอยหลังและเปลี่ยนแปลงตลอดเวลา จนเหลือ 0

### คำถาม 1.2 formatTime() ฟังก์ชันนี้ทำความสำคัญอะไร? ถ้าไม่มีฟังก์ชันนี้ app จะเป็นยังไง

- ตอบ : มีสำคัญในการแปลงเวลาจากวินาทีเป็นนาทีที่อ่านเข้าใจได่ง่าน และถ้าไม่มี formatTime() ถ้าเวลาเป็น 1800 ก็แสดงเป็นแบบนั้นเลยทำให้เข้าใจได้ยาก ทำให้ผู้ใช้ต้องทำความเข้าใจเอง

### คำถาม 1.3 อธิบายข้อแตกต่างระหว่าง setInterval() และ setTimeout()

- ตอบ : setInterval() ใช้สำหรับการทำงาน ซ้ำๆ ทุกๆ ตามระยะเวลาที่กำหนด ไปจนกว่าจะหยุดด้วยคำฟังก์ชัน clearInterval() ส่วน setTimeout() ใช้สำหรับการหน่วงเวลาแล้วทำงาน ครั้งเดียว หลังจากเวลาที่กำหนดไว้ภายใต้คำสั่ง

---

## ส่วนที่ 2: ฟังก์ชันการทำงาน

### คำถาม 2.1 เมื่อผู้ใช้คลิกปุ่ม “▶️ เริ่ม” ครั้งแรก เกิดอะไรขึ้นในลำดับใด (ระบุขั้นตอนอย่างน้อย 4 ขั้น)

- ตอบ : ขั้นตอนแรก 1.Event Listener ทำงาน ปุ่มถูกผูกกับ onclick ="toggleTimer(id)" จะเรียกฟังก์ชัน toggleTimer() พร้อมส่ง idของtask ที่เกี่ยวข้องเข้าไป ขั้นตอนสอง 2.ทำการตรวจสอบสถานะของ task ด้วย tasks.find()พบว่า isRunningเพราะtaskนั้นยังเป็นสถานะfalseเพราะเพิ่งสร้างขึ้นมาใหม่ ขั้นตอนสาม 3.เริ่มต้นการนับเวลาโค้ดเข้าสู่เงื่อนไขการทำงานและตรวจสอบ else ตั้งค่า task.isRunning = true เมื่อเริ่มเวลาสร้างตัวจับเวลา setInterval() ให้ทำงานทุก ๆ 1 วินาทีตามที่ตั้งไว้ ขั้นตอนสี่ 4.การทำงานของ setInterval() โดยทุกๆ 1 วินาที task.timeLeft-- จะถูกลดลงทีละ 1 แต่ถ้าเมื่อtimeLeftลดจนเหลือ 0 หรือหมดเวลา หยุดการทำงานด้วย clearInterval() ตั้งค่า isRunning = false เมื่อเวลาหมด เล่นเสียง alarm (playAlarm()) และแสดง notification ว่าเสร็จสิ้น ขั้นตอนห้า 5.ทุกครั้งที่เวลาลดลง จะเรียก renderTasks() เพื่ออัปเดตหน้าจอให้ผู้ใช้เห็นเวลาที่เหลือแบบเรียลไทม์

### คำถาม 2.2 ในฟังก์ชัน toggleTimer() ทำไมจึงต้องมี clearInterval() ก่อนแล้วจึงตั้ง task.isRunning = false

- ตอบ : clearInterval() คือการหยุดการทำงานจริงของตัวจับเวลาในของส่วน เพื่อป้องกันเวลาใน task ลดลงต่อเนื่องโดยไม่ถูกควบคุมแล้วจึงไปทำในส่วนtask.isRunning = false คือการอัปเดตสถานะของ task ให้ตรงกับความจริง การทำสองขั้นตอนนี้ต่อเนื่องกันทำให้ทั้ง การทำงานจริง และ สถานะที่แสดงผล สอดคล้องกัน ไม่เกิดบั๊กหรือความผิดพลาดใน UI

### คำถาม 2.3 ถ้า timer หมดเวลา จะเกิดความผิดพลาดใดหากไม่มี clearInterval()

- ตอบ : หากไม่มี clearInterval() เมื่อ timer หมดเวลา จะทำให้ตัวจับเวลายังทำงานต่อไปทั้งที่ควรหยุดแล้วทำให้ส่งผลเกิดการทำงานซ้ำซ้อนต่อเนื่องไม่มีหยุดทำให้เกิดปัญหาขึ้นมาได้ เช่น UI กระพริบ, alarm เด้งไม่หยุด, notification ซ้ำ และสิ้นเปลืองทรัพยากรโดยไม่จำเป็น

---

## ส่วนที่ 3: DOM และ UI

### คำถาม 3.1 ฟังก์ชัน renderTasks() สามารถแยกออกเป็นฟังก์ชันย่อยได้กี่ฟังก์ชัน จงเสนอการแยกที่เหมาะสม

- ตอบ : renderTasks() สามารถแยกออกเป็น 4 ฟังก์ชันย่อยหลัก ได้แก่ 1.getTaskStatus(task) ตรวจสอบว่า task เสร็จแล้ว,ใกล้หมดเวลา 2.renderTaskItem(task) รับtask 1 ตัว แล้วคืนค่าHTML string ของtaskเหล่านั้นได้ 3.renderTaskControls(task)สร้างHTMLของปุ่ม “▶️ เริ่ม/⏸️ หยุด” และ “ลบ” โดยตรวจสอบสถานะ isRunning และ isCompleted 4.renderTaskList(tasks)รวมHTMLของทุก task ด้วยการเรียก renderTaskItem() แล้วใส่ลงใน taskList.innerHTML

### คำถาม 3.2 ใช้วิธีใดในการอัปเดต UI ทุกครั้งที่ timer เปลี่ยนแปลง มีวิธีการอื่นนอกเหนือจำนวนปัจจุบันหรือไม่

- ตอบ : ทุกครั้งที่เวลาเปลี่ยนแปลง จะมีการเรียก renderTasks() เพื่ออัปเดต UI ให้ตรงกับสถานะล่าสุดของ task วิธีการอื่นนอกเหนือคือ 1.อัปเดตเฉพาะ DOM element ที่เปลี่ยนแปลง เช่น อัปเดตเฉพาะ <div class="task-timer"> ของ task ที่กำลังนับถอยหลัง 2.ใช้Virtual DOM หรือ Frameworkให้frameworkจัดการ diff และ update เฉพาะส่วนที่เปลี่ยน วิธีการพวกนี้อยู่ที่การปรับเปลี่ยนให้โค้ดมีความหยืดหยุ่นและอัปเดตได้มีประสิทธิภาพมากขึ้น

### คำถาม 3.3 “keypress” event listener ทำความสำคัญอะไร หากลบออกไป app จะเป็นยังไง

- ตอบ : keypress ใช้ใน event listener ทำให้ผู้ใช้สามารถเพิ่ม task ได้ด้วยการกด Enter ซึ่งช่วยให้การใช้งานรวดเร็วและสะดวกขึ้นทำให้การทำงานของหน้าเว็บดูกระชับ แต่ถ้าลบออกไป แอปยังทำงานได้ตามปกติ แต่ผู้ใช้จะต้องคลิกปุ่ม “เพิ่ม” ทุกครั้งแทน ซึ่งอาจทำให้เสียเวลาขึ้นเล็กน้อย

---

## ส่วนที่ 4: Web Audio API

### คำถาม 4.1 อธิบายหน้าที่ของการตั้งค่า:

- **oscillator.frequency.value = 800**
  - ตอบ : กำหนดความถี่ของเสียงที่ oscillator สร้างขึ้นเป็น 800 Hz ความถี่นี้คือระดับเสียงที่ผู้ใช้จะได้ยินตามตัวเลข ยิ่งตัวเลขสูง เสียงยิ่งแหลม, ยิ่งต่ำ เสียงยิ่งทุ้ม
- **oscillator.type = "sine"**
  - ตอบ : กำหนดรูปคลื่นเสียงเป็น sine wave หรือคลื่นไซน์ ให้เสียงที่นุ่มและบริสุทธิ์ที่สุดต่างจาก square, sawtooth, triangle ที่ให้เสียงหยาบหรือมีฮาร์มอนิกมากกว่า
- **gainNode.gain.setValueAtTime(0.3, audioContext.currentTime)**
  - ตอบ : กำหนดระดับความดังของเสียงที่ออกมาเป็น 0.3 (จากช่วง 0.0–1.0) ณ เวลาปัจจุบัน

### คำถาม 4.2 ทำไมเสียง alarm จึงใช้เวลา 0.5 วินาที แล้วหยุด ถ้าเพิ่มเป็น 2 วินาทีจะส่งผลต่อ UX อย่างไร

- ตอบ :

###

- ตอบ :

---
